// Create a for loop that iterates though 101 numbers (from 0 - 100)./ If the current iteration is an Odd number, print "Odd" to the console, otherwise print "Even".
for(i = 0; i < 101; i++){
    if(i % 2 != 0){
        console.log("Odd")
    }
    else(
        console.log("Even")
    )
}